@{
  Layout = "_Layout";
}

@model HairSalon.Models.Stylist

<div class="header">
  <h1>Are you sure you want to remove @Html.DisplayNameFor(model => model.Name) : @Html.DisplayFor(model => model.Name)?
  </h1>
</div>

@using (Html.BeginForm())
{
  <input type="submit" value="Delete" class="btn btn-primary"/>
}

<div class="links">
  @Html.ActionLink("Back to restaurants", "Index")
</div>
